import * as jsonParser from '../utils/json-parser.util';

describe('json-parser.util', () => {
  it('should be defined', () => {
    expect(jsonParser).toBeDefined();
  });

  // ... test các hàm chính khác
}); 